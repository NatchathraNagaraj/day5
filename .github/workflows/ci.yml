name: CI

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check app.sh
        run: bash -n wrong-container.sh

      - name: Build application
        run: docker build -t devops-task-manager .
